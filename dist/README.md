# dist

This directory is reserved for release bundles and generated package artifacts.

Do not treat `dist/` contents as canonical source-of-truth; edit canonical files in `docs/`, `schemas/`, `fixtures/`, and `registry/`.
