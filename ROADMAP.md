## Roadmap and current status (repo-backed)

This roadmap reflects the **actual repository artifacts**, not only the content embedded in the Suite document.
It separates: (A) Spec authoring (text), and (B) Productization (executable, enforceable deliverables).

**Repo snapshot:** AICP-main_v1  
**Suite document:** `AICP_Suite_Overview_and_Skeleton_v0.1.21_EN.md`  
**Core package (current location):** `AICP_Core_v0.1_Normative_Package_0.1.0/`

### Legend
- ✅ **Shipped**: exists in-repo as a standalone artifact and is runnable/usable.
- 🟡 **Drafted**: described in Suite doc, but not yet extracted/productized as standalone artifacts.
- 🔜 **Next**: next milestone to execute.
- ⏳ **Later**: planned, but not in the immediate milestone.

---

## A) Spec authoring status (text completeness)

- ✅ Suite overview & scope framing: present as `AICP_Suite_Overview_and_Skeleton_v0.1.21_EN.md`.
- ✅ Core v0.1 normative (document + schemas + fixtures + golden transcripts): present as `AICP_Core_v0.1_Normative_Package_0.1.0/`.

The following are currently **present in the Suite document as drafted RFC sections**, but **not yet productized** as standalone RFC files / registries / runnable suites:
- 🟡 Registries & change control (RFC)
- 🟡 Error model & recovery (RFC)
- 🟡 EXT-CAPNEG (RFC)
- 🟡 Transport bindings (MCP / HTTP+WS / BUS) (RFC)
- 🟡 EXT-POLICY-EVAL (RFC)
- 🟡 EXT-OBJECT-RESYNC (RFC)
- 🟡 EXT-IDENTITY-LC (RFC)
- 🟡 Applied extensions (workflow / delegation / disputes / security alerts) (RFC)
- 🟡 Governance/IPR & stewardship (RFC)
- 🟡 Interop event plan + external security review frame (RFC)

---

## B) Productization status (implementable + enforceable)

### ✅ Completed (in-repo)
- ✅ Agent-first SDD rails:
  - `AGENTS.md` (root)
  - PR template + Issue templates
  - `Makefile` (`make validate`, `make test`, `make release-check`)
  - Validation scripts (`scripts/`)
  - Baseline CI workflow (`.github/workflows/ci.yml`)
- ✅ Core package deliverables (as a release-style folder):
  - Core normative doc (DOCX)
  - Core JSON Schemas
  - Test vectors (TV)
  - Golden transcripts + public keys

### 🔜 Next milestone (M1): Canonical repo layout (remove versioned folder as source-of-truth)
**Goal:** make the repo itself the system of record, and generate versioned zips only on release.
- Move / promote Core artifacts to canonical paths:
  - `docs/core/` (normative Core docs)
  - `schemas/core/` (Core schemas)
  - `fixtures/` (TV + golden transcripts + keys)
- Keep release packaging under `dist/` (generated by tooling), not as working directories.
- Update README(s) and any references accordingly.
**Acceptance:** `make validate` validates the canonical `fixtures/golden_transcripts` against canonical `schemas/core/...`.

### 🔜 Next milestone (M2): Split the Suite into standalone RFC documents
**Goal:** reduce “monolith doc” risk and make individual components adoptable.
- Create:
  - `docs/suite/` (umbrella overview)
  - `docs/rfc/` (registries, error model, governance, interop/security)
  - `docs/extensions/` (each EXT RFC)
  - `docs/bindings/` (each binding RFC)
**Acceptance:** each RFC is independently readable, linkable, and versioned.

### 🔜 Next milestone (M3): Machine-readable registries as first-class artifacts
**Goal:** prevent collisions and enable third-party ecosystems.
- Create `registry/*.json` per the Registry RFC (message types, profiles, bindings, codes, etc.).
- Add a lightweight `scripts/validate_registry.py` + CI check.
**Acceptance:** registry validation runs in CI; changes are reviewed and collision-free.

### 🔜 Next milestone (M4): Conformance as a product (runner + reports)
**Goal:** “AICP-compatible” becomes measurable.
- Add `/conformance` test catalogs (Core + opt-in EXT suites).
- Add a conformance runner CLI that outputs a **compatibility report** (machine-readable).
**Acceptance:** one command produces pass/fail + report; CI runs Core suite.

### ⏳ Later milestones (productization hardening)
- ⏳ M5 Reference implementations in-repo:
  - `/reference/python` (minimum correct) and optionally `/reference/typescript`
- ⏳ M6 Expand golden transcripts to cover real-world recovery and consent flows:
  - revoke, unknown-base+resync, invalid signature, duplicate message_id, etc.
- ⏳ M7 Plugfest kit + Interop report + Errata process:
  - `/interop/*` artifacts
- ⏳ M8 External security review artifacts and remediation log:
  - `/security_review/*`
- ⏳ M9 Release Candidate → v0.1.0:
  - feature freeze, registry snapshot, final compatibility marks, release packaging, checksums

---

## Immediate next step
**M1 (Canonical repo layout)** is the next priority. It unlocks all subsequent milestones by making CI, validation, and adoption workflows stable.
