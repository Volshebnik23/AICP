# AICP Core v0.2 TypeScript/JavaScript drop-in

This explicitly versioned, experimental post-UAT example generates
`CONTRACT_PROPOSE → CONTRACT_ACCEPT → ATTEST_ACTION` with an exact contract hash,
proposal message binding, active-head reference, message hashes, and hash chain.

It does not replace the default Core v0.1 drop-in.
