# TEST-ONLY cryptographic keys

`fixtures/keys/TEST_private_keys.json` contains deterministic **test-only** Ed25519 private keys used to generate reproducible signed fixtures.

These keys are intentionally non-secret and MUST NOT be used for production traffic, identity, or security decisions.
