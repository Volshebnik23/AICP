# Delegated identity fixtures

These fixtures are generated deterministically via:

```bash
python scripts/generate_delegated_identity_fixtures.py
```

This ensures canonical `message_hash`/`prev_msg_hash` and reproducible Ed25519 signatures.
