# Reception chat semantics fixtures

These fixtures are generated deterministically via:

```bash
python scripts/generate_reception_chat_semantics_fixtures.py
```

Use this generator when updating transcripts so `message_hash`/`prev_msg_hash` bindings remain canonical and reproducible.
