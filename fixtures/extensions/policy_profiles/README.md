# Policy semantic profile fixtures (M25)

These fixtures are generated deterministically with:

- `python scripts/generate_policy_semantic_profile_fixtures.py`

Why they changed:
- Add executable M25 profile vectors for OPA/Rego, ABAC/RBAC, and LLM-safety semantic interoperability boundaries over `EXT-POLICY-EVAL`.
