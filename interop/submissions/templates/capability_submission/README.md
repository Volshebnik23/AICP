# Capability submission template

Replace every placeholder and attach a report produced in
`full-capability` mode. A real `implements_capability` submission must use
`evidence_status: reproducible`; the template status is intentionally not
publication-eligible.
