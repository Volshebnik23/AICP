# Binding submission template

Replace every placeholder and attach a report-format 2.2 result produced in
`full-binding` mode for both roles of the same implementation build. A real
`implements_binding` package uses `evidence_status: reproducible`; this template is
instructional, is not an external demonstration, and does not establish pairwise
interoperability.
