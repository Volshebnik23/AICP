# Example only: pairwise profile interoperability claim

This folder is an **example submission package**, not a real third-party interoperability result.

It demonstrates the shape of a pairwise interoperability manifest using fictional implementation identifiers and local repo evidence references.
