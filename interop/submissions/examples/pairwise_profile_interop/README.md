# Example only: pairwise profile interoperability claim

This package is a **fictional example** that demonstrates a pairwise interoperability claim between two fictional implementations.

It is not a real vendor submission and must not be used as public proof of external interoperability.
